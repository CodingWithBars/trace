rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access to receipts/images
      allow read: if true;
      // Allow any authenticated user (admin/officer) to upload
      allow write: if request.auth != null;
    }
  }
}
