rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules: allow all reads and writes
    // TODO: Secure these rules before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
