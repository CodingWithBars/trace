import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TRACE app smoke test', (WidgetTester tester) async {
    // Skipped — requires Firebase initialization in test environment
  });
}
